# Tasks

## General differentiable world model — active

- [x] Synchronize the canonical checkout with verified GitHub `main`.
- [x] Preserve the exact qualified source as a verified local Git bundle and
  GitHub archive tag before deleting temporary clones.
- [x] Archive compact run metadata, the historical compatibility fixture, and
  qualified toy evidence; remove the 7.6-GiB stale run/checkpoint tree.
- [x] Remove repository caches, generated demos, disposable temp caches, and
  selected obsolete source clones.
- [x] Remove zero-live-reference rejected profiles and campaign-specific tools
  from the active public surface.
- [x] Preserve superseded status/task/training/accuracy ledgers in Git history
  and the ignored local evidence archive; keep active tracked memory concise.
- [x] Implement and unit-test a differentiable weighted exact free-motion fit
  over a bounded RGB-derived history.
- [x] Freeze the new temporal-rung source, seed namespaces, horizons, gates,
  latency protocol, and artifact schema before selector access.
- [x] Pass focused implementation/static checks without materializing any
  protected episode.
- [x] Pass the final source gate: `1075 passed, 16 skipped`, Ruff lint and
  format-check over 224 Python files, compileall, specification version, and
  diff integrity.
- [ ] Commit and push the reviewed source so development and qualification can
  bind the exact same clean Git provenance.
- [ ] Run the development-only 32-update training and 16-seed audit; inspect
  and independently hash the report and weights-only checkpoint.
- [ ] If and only if development passes, run selector and confirmation once
  from the independently hashed checkpoint without changing source.
- [ ] Materialize and inspect the temporal rung's final manifest once only if
  every earlier gate passes.
- [ ] Bridge the qualified inverse-rendering and temporal estimator into the
  public one-slot `OnlineWorldModel` observation/belief/rollout workflow.
- [ ] Add one complexity per later rung: camera motion; drag identification;
  RGB-D; variable scale; multi-object sets; identity/occlusion; contact;
  material parameters; known actions/planning; richer modalities/geometry.
- [ ] At every rung require state/velocity/horizon accuracy, uncertainty,
  support, gradient, memory, and throughput non-regression; stop failed
  architecture families instead of marginal retuning.
- [ ] After the rung passes, update its exact evidence, rerun proportional
  gates, commit and push the reviewed branch, and fast-forward GitHub `main`.

Historical completed and rejected work through specification 1.51 is retained
in Git commit `c16acc99` and the local pre-generalization archive.
